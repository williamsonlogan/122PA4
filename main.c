#include "grocery.h"

int main(void) {
  // init queue(didn't see a need for a function)
  Queue pQueue;
  pQueue.pHead = NULL;
  pQueue.pTail = NULL;

  // vars

  // sim loop
  printf("Hello world!");
}
